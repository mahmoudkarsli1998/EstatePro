import { ENABLE_3D } from '../../config/performance';

export default function Hero3DOverlay() {
  if (!ENABLE_3D) return null;

  return null; // Original code would be here, but we are keeping it hidden/disabled.
}
